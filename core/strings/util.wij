module strings;

use core:mem;

type String = {
    buffer: *[byte],
    length: int
}

foreign {
    // transmute: fn(src: 'a) -> 'b;
    // strbytes: fn(string: str) -> *[byte];
}

pub fn new() {
    return;
}
